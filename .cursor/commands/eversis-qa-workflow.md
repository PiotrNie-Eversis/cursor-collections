# eversis-qa-workflow (Cursor Collections — QA practice)

You are running the **QA workflow** from **Eversis Cursor Collections** — test planning, regression, AC verification, and quality reporting. **Not** the Implement **Fine handoff** (`@eversis-fine-handoff`).

## Load the canonical prompt (required)

1. If **`@eversis-qa-workflow`** is **already attached** with its full workflow section, you may skip re-reading.
2. Otherwise **read** `.cursor/prompts/public/eversis-qa-workflow.md` in full.

Load QA skills on demand from `.cursor/skills/eversis-*/SKILL.md` or **`eversis-collections` MCP** (`eversis_skills_get`).

Enable **Atlassian MCP** when delivering artifacts to Jira or Confluence.

## Your input (fill below, then send)

Jira ticket / feature scope, optional flow keyword (`plan testing`, `regression`, `verify ac`, `report bug`, `quality health`, `audit accessibility`), and delivery preference.
